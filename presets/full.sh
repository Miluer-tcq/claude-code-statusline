#!/bin/bash
# Preset: full — 3 lines, complete information
# L1: Model + Effort + Think + Fast + Perm + Version + Active
# L2: Context bar + Skills + MCP + CWD + Git
# L3: Ctx tokens + Sum tokens + Duration + Cost

MODULES_L1=("model" "effort" "think" "fast" "perm" "version" "active")
MODULES_L2=("context" "skills" "mcp" "cwd" "git")
MODULES_L3=("ctx_tokens" "sum_tokens" "duration" "cost")
PRESET_LINES=3
